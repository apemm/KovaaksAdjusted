"""Performance optimizer: free Process Lasso replacement for KovaaK's.

Hardware detection, system checkup with per-item fixes, a game watchdog that
re-applies priority/affinity on every launch, launch-option recommendations,
and hardware-matched settings advice. Windows-only at runtime; every module
imports cleanly on any OS (probes return unknowns elsewhere) so the test
suite stays cross-platform.
"""
