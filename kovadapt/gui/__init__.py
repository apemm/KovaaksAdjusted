"""kovadapt GUI package (PySide6 + pyqtgraph; install with kovadapt[gui]).

Import nothing heavy here — `kovadapt.gui.app` pulls in Qt on demand so the
core package stays importable without GUI dependencies.
"""
